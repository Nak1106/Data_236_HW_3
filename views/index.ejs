<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ADS SJSU — Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">ADS SJSU</a>
      <div class="navbar-nav ms-auto">
        <% if (user) { %>
          <a class="nav-link" href="/dashboard">Dashboard</a>
          <a class="nav-link" href="/logout">Logout</a>
        <% } else { %>
          <a class="nav-link" href="/login">Login</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow">
          <div class="card-body p-4">
            <h1 class="mb-3 text-center">Department of Applied Data Science</h1>
            <p class="text-center mb-4">Welcome to ADS SJSU sample app with session based auth</p>
            <div class="d-flex justify-content-center gap-2">
              <% if (user) { %>
                <a class="btn btn-primary" href="/dashboard">Go to Dashboard</a>
                <a class="btn btn-outline-secondary" href="/logout">Logout</a>
              <% } else { %>
                <a class="btn btn-primary" href="/login">Login</a>
              <% } %>
            </div>
            <% if (message) { %>
              <div class="alert alert-info mt-3 text-center"><%= message %></div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
